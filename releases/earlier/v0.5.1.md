# v0.5.1 — Maintenance

A small maintenance release on top of v0.5.0.

## Changes

- Fine-tuning and precision fixes for recent work. If you know, you know.
- No new features, no changed settings, nothing to configure.

Still looking? Corners are underrated. Music helps.

## Upgrade note

Client-side only — a plain `git pull` (or pulling the new image tag) is
enough. Hard-refresh the browser after upgrading.
