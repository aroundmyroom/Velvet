# Velvet v0.0.6

## Fixes
- Docker migration now correctly renames legacy `mstream.sqlite` to `velvet.sqlite` on first boot when the new database file is absent, preserving user metadata such as starred titles.
- Restore/import now accepts legacy backup archives that still contain `mstream.sqlite`.

## Docs
- Install and backup docs now describe the SQLite migration and legacy restore compatibility.
